/**
 * HOME PAGE - PÁGINA RAÍZ
 * 
 * No hacer nada aquí. El middleware se encarga de redirigir:
 * - Si está autenticado → /dashboard
 * - Si NO está autenticado → /login
 * 
 * Esta página nunca se renderiza realmente.
 */

export default function HomePage() {
  return null;
}